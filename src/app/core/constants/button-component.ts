export enum ButtonVariant {
    flat = 'flat',
    stroked = 'stroked',
    raised = 'raised',
    icon = 'icon',
}

export enum ButtonType {
    button = 'button',
    submit = 'submit',
    reset = 'reset',
}
